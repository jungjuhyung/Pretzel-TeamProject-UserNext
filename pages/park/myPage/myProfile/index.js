"use client";

import { ProfileContainer, ProfileImage } from "@/styles/park/myPage/myProfile";

const MyProfile = () => {
    return (
        <ProfileContainer>
            <ProfileImage />
        </ProfileContainer>
    );
};

export default MyProfile;
